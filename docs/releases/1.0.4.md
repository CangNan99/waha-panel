# waha-panel 1.0.4

发布日期：2026-09-19（Asia/Shanghai）

## 本次更新

- 为 `waha-panel` Compose 服务设置 `256m` 内存上限，避免 Argon2 管理员认证在低内存容器中因无法分配 `65536 KiB` 而返回 HTTP 500。
- 面板镜像与安装默认版本升级为 `1.0.4`。
- 保留 `1.0.3` 镜像标签，可用于回滚。

## 明确不变

- WAHA 镜像、会话和媒体卷不变。
- 面板 SQLite 数据卷不变。
- Docker 内部网络、端口和反向代理结构不变。
